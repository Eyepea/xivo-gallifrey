diff -Nurp asterisk-1.2.27.orig/res/res_agi.c asterisk-1.2.27/res/res_agi.c
--- asterisk-1.2.27.orig/res/res_agi.c	2007-07-30 20:40:54.000000000 +0200
+++ asterisk-1.2.27/res/res_agi.c	2008-05-14 18:51:21.000000000 +0200
@@ -131,7 +131,7 @@ static void agi_debug_cli(int fd, char *
 	FastAGI defaults to port 4573 */
 static int launch_netscript(char *agiurl, char *argv[], int *fds, int *efd, int *opid)
 {
-	int s;
+	int i, s;
 	int flags;
 	struct pollfd pfds[1];
 	char *host;
@@ -217,6 +217,10 @@ static int launch_netscript(char *agiurl
 	if (!ast_strlen_zero(script))
 		fdprintf(s, "agi_network_script: %s\n", script);
 
+	/* argv[1] is either a string or NULL, but always exists */
+	for (i = 1; argv[i] != NULL; i++)
+		fdprintf(s, "agi_arg_%d: %s\n", i, argv[i]);
+
 	if (option_debug > 3)
 		ast_log(LOG_DEBUG, "Wow, connected!\n");
 	fds[0] = s;
