[general]
static = yes
writeprotect = yes
country = fr
priorityjumping = no

[globals]

#include extensions_macro_incoming_user.conf
#include extensions_macro_outgoing_user.conf

[to-extern]
exten = _X.,1,Macro(outgoing_user)

exten = t,1,Hangup()
exten = h,1,Hangup()
exten = i,1,Playback(fr/extra/user-invalid)

[from-extern]
switch = Realtime/did-extensions@extensions

exten = h,1,Hangup()
exten = t,1,Hangup()

#exec /usr/share/asterisk/bin/create-context-hints
