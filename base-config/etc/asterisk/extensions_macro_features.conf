[macro-recsnd]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Answer()
exten = s,n,Set(RECORDFILE=${ARG1}/${REAL_SRCNUM}-${EPOCH})
exten = s,n,Playback(record-message-after-beep)
exten = s,n,Record(${RECORDFILE}:${ARG2})
exten = s,n,Wait(1)
exten = s,n,Playback(${RECORDFILE})
exten = s,n,Hangup()

[macro-phonestatus_say_enabled_disabled]
exten = s,1,Playback(${ARG1})
exten = s,n,GotoIf(${ARG2}?enabled,1:disabled,1)

exten = enabled,1,Playback(on)
exten = enabled,n,GotoIf(${ARG3}?to,1)

exten = to,1,Playback(to)
exten = to,n,SayDigits(${ARG3})

exten = disabled,1,Playback(off)

[macro-phonestatus_testvm]
exten = s,1,MailboxExists(${REAL_SRCNUM}@${REAL_CONTEXT})
exten = s,n,GotoIf($["${VMBOXEXISTSSTATUS}" = "SUCCESS"]?exit)
exten = s,n,Set(XIVO_ENABLEVOICEMAIL=)
exten = s,n(exit),MacroExit()

; TODO: say something smarter if no feature has been enabled.
[macro-phonestatus]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Answer()
exten = s,n,AGI(phonestatus)
exten = s,n,Playback(status-phone)
exten = s,n,GotoIf($["${XIVO_ENABLEUNC}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,forward-inc,${XIVO_ENABLEUNC},${XIVO_DESTUNC})
exten = s,n,GotoIf($["${XIVO_ENABLEBUSY}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,forward-busy,${XIVO_ENABLEBUSY},${XIVO_DESTBUSY})
exten = s,n,GotoIf($["${XIVO_ENABLERNA}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,forward-rna,${XIVO_ENABLERNA},${XIVO_DESTRNA})
exten = s,n,Macro(phonestatus_testvm)
exten = s,n,GotoIf($["${XIVO_ENABLEVOICEMAIL}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,vm-status,${XIVO_ENABLEVOICEMAIL})
exten = s,n,GotoIf($["${XIVO_CALLFILTER}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,screening-status,${XIVO_CALLFILTER})
exten = s,n,GotoIf($["${XIVO_CALLRECORD}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,record-status,${XIVO_CALLRECORD})
exten = s,n,GotoIf($["${XIVO_ENABLEDND}"=""]?$[${PRIORITY} + 2])
exten = s,n,Macro(phonestatus_say_enabled_disabled,dnd-status,${XIVO_ENABLEDND})
exten = s,n,Playback(bye)
exten = s,n,Hangup()

[macro-fwdundoall]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(fwdundoall)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|unc|0|)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|rna|0|)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|busy|0|)
exten = s,n,Answer()
exten = s,n,Playback(all-forward-off)
exten = s,n,Hangup()

[macro-fwdundounc]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,unc,0)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|unc|0|)
exten = s,n,Answer()
exten = s,n,Playback(forward-off)
exten = s,n,Hangup()

[macro-fwdunc]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,unc,1,${ARG1})
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|unc|1|${ARG1})
exten = s,n,Answer()
exten = s,n,Playback(forward-on)
exten = s,n,Hangup()

[macro-fwdundorna]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,rna,0)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|rna|0|)
exten = s,n,Answer()
exten = s,n,Playback(forward-off)
exten = s,n,Hangup()

[macro-fwdrna]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,rna,1,${ARG1})
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|rna|1|${ARG1})
exten = s,n,Answer()
exten = s,n,Playback(forward-on)
exten = s,n,Hangup()

[macro-fwdundobusy]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,busy,0)
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|busy|0|)
exten = s,n,Answer()
exten = s,n,Playback(forward-off)
exten = s,n,Hangup()

[macro-fwdbusy]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,busy,1,${ARG1})
exten = s,n,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|busy|1|${ARG1})
exten = s,n,Answer()
exten = s,n,Playback(forward-on)
exten = s,n,Hangup()

[macro-enablevm]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,MailboxExists(${REAL_SRCNUM}@${REAL_CONTEXT})
exten = s,n,GotoIf($["${VMBOXEXISTSSTATUS}" = "FAILED"]?novmbox,1)
exten = s,n,AGI(user_set_feature,vm)
exten = s,n,Answer()
exten = s,n,GotoIf(${XIVO_VMENABLED}?vmenabled,1:vmdisabled,1)

exten = novmbox,1,Hangup()

exten = vmenabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|vm|1|)
exten = vmenabled,n,Playback(vm-on)
exten = vmenabled,n,Hangup()

exten = vmdisabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|vm|0|)
exten = vmdisabled,n,Playback(vm-off)
exten = vmdisabled,n,Hangup()

[macro-enablednd]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,dnd)
exten = s,n,Answer()
exten = s,n,GotoIf(${XIVO_DNDENABLED}?dndenabled,1:dnddisabled,1)

exten = dndenabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|dnd|1|)
exten = dndenabled,n,Playback(dnd-on)
exten = dndenabled,n,Hangup()

exten = dnddisabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|dnd|0|)
exten = dnddisabled,n,Playback(dnd-off)
exten = dnddisabled,n,Hangup()

[macro-incallrec]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,callrecord)
exten = s,n,Answer()
exten = s,n,GotoIf(${XIVO_CALLRECORDENABLED}?callrecordenabled,1:callrecorddisabled,1)

exten = callrecordenabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|callrecord|1|)
exten = callrecordenabled,n,Playback(record-call-on)
exten = callrecordenabled,n,Hangup()

exten = callrecorddisabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|callrecord|0|)
exten = callrecorddisabled,n,Playback(record-call-off)
exten = callrecorddisabled,n,Hangup()

[macro-incallfilter]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,AGI(user_set_feature,callfilter)
exten = s,n,Answer()
exten = s,n,GotoIf(${XIVO_CALLFILTERENABLED}?callfilterenabled,1:callfilterdisabled,1)

exten = callfilterenabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|callfilter|1|)
exten = callfilterenabled,n,Playback(screening-on)
exten = callfilterenabled,n,Hangup()

exten = callfilterdisabled,1,UserEvent(Feature,AppData: ${REAL_CONTEXT}|${REAL_SRCNUM}|callfilter|0|)
exten = callfilterdisabled,n,Playback(screening-off)
exten = callfilterdisabled,n,Hangup()

[macro-agentstaticlogin]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Answer()
exten = s,n,AgentCallBackLogin(${ARG1}||${REAL_SRCNUM}@${REAL_CONTEXT})
exten = s,n,Hangup()

[macro-agentstaticlogoff]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Set(LANGUAGE()=${ARG2})
exten = s,n,System(/usr/sbin/asterisk -rx "agent logoff Agent/${ARG1}")
exten = s,n,Answer()
exten = s,n,Playback(agent-loggedoff)
exten = s,n,Hangup()

[macro-calllistening]
exten = s,1,Answer()
exten = s,n,Authenticate(916735)
exten = s,n,ChanSpy()
exten = s,n,Hangup()

[macro-vmdelete]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Answer()
exten = s,n,VMauthenticate(${REAL_SRCNUM}@${REAL_CONTEXT})
exten = s,n,System(/bin/rm -f ${ARG1}/${REAL_CONTEXT}/${REAL_SRCNUM}/*/* || /bin/true)
exten = s,n,PlayBack(vm-deleted)
exten = s,n,Hangup()

[macro-voicemsg]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Answer()
exten = s,n,AGI(voicemsg)
exten = s,n,VoiceMailMain(${REAL_SRCNUM}@${REAL_CONTEXT},${XIVO_VMOPTIONS})
exten = s,n,Hangup()

[macro-handynumbers]
exten = s,1,Set(REAL_SRCNUM=${CALLERID(num)})
exten = s,n,Set(REAL_DSTNUM=${MACRO_EXTEN})
exten = s,n,Set(REAL_CONTEXT=${MACRO_CONTEXT})
exten = s,n,Set(REAL_EXTENPATTERN=${ARG1})
exten = s,n,AGI(handynumbers)
exten = s,n,GotoIf(${XIVO_INTERFACE}?:error,1)
exten = s,n,Dial(${XIVO_INTERFACE}/${XIVO_TRUNKEXTEN})

exten = error,1,NoOp(Unable to fetch user features, this is a bug)
exten = error,n,Hangup

[macro-ring_type_set]
exten = s,1,GotoIf(${XIVO_RINGTYPE_ALREADY_SET}?exit)
exten = s,n,AGI(getring)
exten = s,n,GotoIf(${XIVO_RINGTYPE}?:exit)
exten = s,n,SIPAddHeader("Alert-Info:<http://www.proformatique.com>\;info=${XIVO_RINGTYPE}")
exten = s,n,Set(XIVO_RINGTYPE_ALREADY_SET=1)
exten = s,n(exit),MacroExit()

; Work around a bug in the Thomson phones concerning originate calls.
; Anyway, each time an AMI Originate is issued, the CALLERID(num) is the wrong one
; so that we need to set it back to the right XIVO_ORIGSRCNUM / XIVO_ORIGSRCNAME one.
[macro-incoming_user_handle_thomson_callerid]
exten = s,1,GotoIf(${XIVO_ORIGSRCNUM}?:exit)
exten = s,n,Set(CALLERID(num)=${XIVO_ORIGSRCNUM})
exten = s,n,Set(CALLERID(name)=${XIVO_ORIGSRCNAME})
exten = s,n(exit),MacroExit()

; Force Thomson phones to remain silent while ringing.
;
; As this macro can be called multiple times for the same channel, and Thomson
; phones are known to bug when the header line added by this macro is present
; several times, the XIVO_ALREADYSILENT macro is set so that consecutive calls
; do not attempt to update the headers again.
;[macro-incoming_user_make_thomson_silent]
;exten = s,1,GotoIf(${XIVO_ISTHOMSON}?:exit)
;exten = s,n,GotoIf(${XIVO_ALREADYSILENT}?exit)
;exten = s,n,SIPAddHeader("Alert-Info:<http://www.proformatique.com>\;info=Silent")
;exten = s,n,Set(XIVO_ALREADYSILENT=1)
;exten = s,n(exit),MacroExit()

[macro-txfax]
exten = s,1,TxFax(${FAXDIR}/${FAXID}.tif|caller|debug)

exten = h,1,UserEvent(_txfax|XIVO_FAXSTATUS: ${FAXID}|${PHASEESTATUS}|${REMOTESTATIONID})
