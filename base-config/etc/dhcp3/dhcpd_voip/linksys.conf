class "LinksysSPA400" {
    match if option vendor-class-identifier = "LINKSYS SPA400";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-400"));
}

class "LinksysSPA901" {
    match if option vendor-class-identifier = "LINKSYS SPA-901";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-901"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA921" {
    match if option vendor-class-identifier = "LINKSYS SPA-921";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-921"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA922" {
    match if option vendor-class-identifier = "LINKSYS SPA-922";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-922"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA941" {
    match if option vendor-class-identifier = "LINKSYS SPA-941";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-941"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA942" {
    match if option vendor-class-identifier = "LINKSYS SPA-942";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-942"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA962" {
    match if option vendor-class-identifier = "LINKSYS SPA-962";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-962"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
}

class "LinksysSPA2102" {
    match if option vendor-class-identifier = "LINKSYS SPA-2102";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-2102"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
    option bootfile-name "Linksys/spa2102.cfg";
}

class "LinksysSPA3102" {
    match if option vendor-class-identifier = "LINKSYS SPA-3102";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-3102"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
    option bootfile-name "Linksys/spa3102.cfg";
}

class "LinksysSPA8000" {
    match if option vendor-class-identifier = "LINKSYS SPA8000";
    log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys SPA-8000"));
    option tftp-server-name = config-option VOIP.tftp-server-name;
    option bootfile-name "Linksys/spa8000.cfg";
}

subclass "voip-mac-address-prefix" 1:00:0c:41 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:0c:41"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:0e:08 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:0e:08"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:0f:66 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:0f:66"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:12:17 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:12:17"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:13:10 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:13:10"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:14:bf {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:14:bf"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:16:b6 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:16:b6"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:18:39 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:18:39"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:18:f8 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:18:f8"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:1a:70 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:1a:70"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:1c:10 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:1c:10"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:1d:7e {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:1d:7e"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:1e:e5 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:1e:e5"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:21:29 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:21:29"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:22:6b {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:22:6b"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:23:69 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:23:69"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:00:25:9c {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:00:25:9c"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}

subclass "voip-mac-address-prefix" 1:68:7f:74 {
    if not exists vendor-class-identifier {
        log(concat("[", binary-to-ascii(16, 8, ":", hardware), "] ", "BOOT Linksys PREFIX 1:68:7f:74"));
        option tftp-server-name = config-option VOIP.tftp-server-name;
    }
}
