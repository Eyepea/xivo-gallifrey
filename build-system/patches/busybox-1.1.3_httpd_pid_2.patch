diff -Nurp busybox-1.1.3.orig/networking/httpd.c busybox-1.1.3/networking/httpd.c
--- busybox-1.1.3.orig/networking/httpd.c	2006-07-11 11:49:57.000000000 +0200
+++ busybox-1.1.3/networking/httpd.c	2006-07-11 11:50:25.000000000 +0200
@@ -2153,6 +2153,15 @@ int httpd_main(int argc, char *argv[])
 # if !DEBUG
   if (daemon(1, 0) < 0)     /* don`t change curent directory */
 	bb_perror_msg_and_die("daemon");
+
+  char *pidfilename = "/var/run/httpd.pid";
+  FILE *pidfile = fopen(pidfilename, "w");
+
+  if (pidfile == NULL)
+	bb_perror_msg_and_die("can't open %s", pidfilename);
+
+  fprintf(pidfile, "%d\n", getpid());
+  fclose(pidfile);
 # endif
   return miniHttpd(server);
 #else
