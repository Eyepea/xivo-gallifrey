diff -Nurp libgpg-error-1.3.orig/src/gpg-error.c libgpg-error-1.3/src/gpg-error.c
--- libgpg-error-1.3.orig/src/gpg-error.c	2005-12-18 16:24:34.000000000 +0100
+++ libgpg-error-1.3/src/gpg-error.c	2006-07-21 13:29:33.000000000 +0200
@@ -64,17 +64,14 @@ static void drop_locale_dir (char *local
 static void
 i18n_init (void)
 {
-  char *locale_dir;
-
 #ifdef ENABLE_NLS
+  char *locale_dir;
 # ifdef HAVE_LC_MESSAGES
   setlocale (LC_TIME, "");
   setlocale (LC_MESSAGES, "");
 # else
   setlocale (LC_ALL, "" );
 # endif
-#endif
-
   locale_dir = get_locale_dir ();
   if (locale_dir)
     {
@@ -82,6 +79,7 @@ i18n_init (void)
       drop_locale_dir (locale_dir);
     }
   textdomain (PACKAGE);
+#endif
 }
 
 
