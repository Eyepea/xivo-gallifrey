#!/usr/bin/make -f
# -*- mode: makefile; coding: utf-8 -*-
#
# $Revision$
# $Date$

# path temporarily gruiked
include ../../../trunk/autobuild.mk
include ../../../trunk/autobuild-classic.mk

DEB_PKG = "pf-xivo-web-interface"
DEB_TB_DEPS = "php4-eaccelerator php-eaccelerator-common php4-cli"
DEB_TB_SCRAMBLED = "1"
TARBALL_DIR := $(shell mktemp -d)


default:

prepare-tarball::
	@cp -a . ${TARBALL_DIR}
	@if [ -z "${FORCE_UNSCRAMBLED}" ]; then \
		eaccelerator_encoder . -r -f -sphp -sinc -o ${TARBALL_DIR}; \
	fi

clean-tarball::
	@rm -rf ${TARBALL_DIR}

