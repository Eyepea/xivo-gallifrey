; title_content_name
DHCP

; fm_active
Active:

; fm_pool_start
Pool start:

; fm_pool_end
Pool end:

; fm_interfaces
Extra network interfaces:

; help_interfaces
DHCP server listen by default on the VoIP network interface. You can add supplementary interfaces on which DHCP server will listen too.

; ipaddr(callback)
Invalid IP address

; ipaddr(empty)
You must fill IP address
